 <div class="container_fluid">
  <div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-10">
      <h4 class="sub-title", style="margin: 20px 0 40px 0;">通知</h4>
      <% if @notifications.exists? %>
        <div class="users-index">
          <%= render @notifications %>
        </div>
      <% else %>
        <p>通知はありません</p>
      <% end %>
    </div>
  </div>
</div>